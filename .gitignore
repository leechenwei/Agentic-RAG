# Secrets — never commit
.env
.env.local

# Vector DB (build artifact — regenerated by running ingest)
chroma_db/

# Stale eval history (now lives in per-session state, not on disk)
eval/eval_runs.jsonl

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
